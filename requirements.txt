# DHVT-T CIFAR-100 Scratch — Dependencies
#
# 설치:
#   pip install -r requirements.txt
#
# CUDA 11.8 wheel 을 명시적으로 원하면 아래 명령으로 torch 를 먼저 설치:
#   pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt

torch>=2.0
torchvision>=0.15
# timm 은 data augmentation (RandAugment, RandomErasing) 에만 사용.
# 모델 architecture 는 외부에서 일절 불러오지 않음 (과제 제약).
timm>=0.9
numpy>=1.24
tqdm>=4.64
